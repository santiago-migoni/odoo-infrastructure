---
name: makefile
code: TASKS-008
version: R00
date: 2026-07-13
---

# Tasks: Makefile — interfaz operativa única

## Phase 1: Setup

- [x] T001 [setup] Crear `Makefile` en la raíz: variables `COMPOSE_PROD`/`COMPOSE_STAGING`/`COMPOSE_EDGE`/`COMPOSE_MONITORING`/`COMPOSE_BACKUP` (`docker compose -f Docker/docker-compose.<stack>.yml`), `.PHONY` declarado, `.DEFAULT_GOAL := help` (target `help` se escribe en T007)

## Phase 2: Targets generados regulares (US1)

- [x] T002 [US1] En el `Makefile`, declarar la matriz de servicios por stack según la tabla de `docs/infrastructure-design.md`: `PROD_SERVICES = db pgbouncer odoo`, `STAGING_SERVICES = db pgbouncer odoo-staging postgres-exporter`, `EDGE_SERVICES = traefik cloudflared`, `MONITORING_SERVICES = prometheus grafana loki promtail cadvisor node-exporter postgres-exporter-prod`, `BACKUP_SERVICES = backup`
- [x] T003 [US1] Depende de T002 — Escribir el template `define svc-target` + `$(foreach)` que genera `<stack>-<svc>-up`, `-stop`, `-restart`, `-logs` para cada `(stack, svc)` de las 5 listas de T002 (aplica a todos los servicios, sin excepción, por la clarification de enumeración completa)
- [x] T004 [US1] Depende de T002 — Escribir el template de `pull` (`<stack>-<svc>-pull`) solo para servicios de imagen oficial: excluye `odoo` (prod), `odoo-staging` (staging) y `backup` (backup) — los 3 con build propio
- [x] T005 [US1] Escribir los targets `rebuild` explícitos (no generados, build propio): `prod-odoo-rebuild` (`$(COMPOSE_PROD) build --no-cache odoo && $(COMPOSE_PROD) up -d odoo`), `staging-odoo-staging-rebuild` (mismo patrón contra `odoo-staging`)
- [x] T006 [US1] Escribir los compuestos de stack: `prod-up`/`prod-down`/`prod-status`/`prod-logs`, y el mismo cuarteto para `staging`, `edge`, `monitoring`, `backup` (`up -d`/`down`/`ps`/`logs -f` sobre el compose completo del stack)
- [x] T007 [US1] Escribir el target `help`: imprime los compuestos de stack + los especiales de T008/T011 con una descripción corta, y la matriz servicios×stack de T002 para que el patrón `<stack>-<svc>-<acción>` sea descubrible sin leer el Makefile

## Phase 3: Targets especiales — wiring de lo existente (US1, US3)

- [x] T008 [US1] Escribir en el `Makefile` los targets especiales que llaman scripts/compose ya existentes, sin reimplementar lógica: `staging-up` → `./scripts/staging-up.sh`; `staging-down` → `./scripts/staging-down.sh`; `staging-extend` → `./scripts/staging-extend.sh`; `staging-db-restore` → alias de `staging-up` (documentado en el propio target: el refresh de staging es el ciclo completo restore+anonimización, nunca un restore parcial); `backup-backup-run` (+ alias `backup`) → `$(COMPOSE_BACKUP) run --rm backup`; `setup-backup-role` → `./scripts/setup-backup-role.sh`; `setup-monitoring-role` → `./scripts/setup-monitoring-role.sh`. **Bug real encontrado y corregido**: el compuesto genérico de T006 (`stack-target`) generaba `staging-up`/`staging-down` como `docker compose up -d`/`down` crudo, colisionando con estos especiales (`make -n` mostró `warning: overriding commands for target 'staging-up'`) — un `up` crudo hubiera arrancado Odoo sin restore/anonimización, violando el invariante de 005. Corregido en T006: staging usa un template `stack-readonly-target` que solo genera `status`/`logs` (solo lectura); `up`/`down` de staging son *exclusivamente* los especiales de este task. Verificado sin warnings tras el fix.

## Phase 3.5: Inconsistencia de casing `Docker/` vs `docker/` (hallazgo fuera de alcance, corregido con confirmación del usuario)

- [x] T008b Encontrado al leer `scripts/staging-up.sh` para modelar T009: un commit del usuario (`fec8884 "fix: corregir rutas de archivos en Docker y scripts"`, ya en `main` antes de arrancar esta feature) había cambiado toda referencia al directorio `Docker/` (mayúscula, el nombre real que git rastrea desde 007) a `docker/` (minúscula) en 13 archivos — pero sin renombrar el directorio en sí. En este Mac (filesystem case-insensitive) esto no se nota; en `serverdipleg` (Linux real, case-sensitive) cada `docker compose -f docker/docker-compose.X.yml` habría fallado con "file not found". Presentado al usuario con `AskUserQuestion`; eligió estandarizar en minúscula. **Aplicado**: `git mv Docker DockerTMP && git mv DockerTMP docker` (rename en dos pasos para forzar el reconocimiento en un filesystem case-insensitive); corregidas las 6 referencias residuales en `docs/infrastructure-design.md` (no eran historial congelado, describen el estado actual) y un typo suelto del mismo commit (paréntesis de más en un `echo` de `staging-up.sh`). El propio `Makefile` (T001-T007, escrito con `Docker/` antes de este hallazgo) también corregido a `docker/`. Verificado con build real de ambas imágenes y `docker compose config -q` en las 5 stacks desde la ruta minúscula. `CHANGELOG.md` (entrada histórica de 007) no se toca — describe correctamente el estado de esa fecha.

## Phase 4: Restore de prod (US2)

- [x] T009 [US2] Escribir `scripts/restore-prod.sh` (corre dentro de `Docker/Dockerfile.backup` vía `--entrypoint`, patrón gemelo de `scripts/restore-staging.sh` sin anonimización): `restic restore latest --no-lock` desde `$RESTIC_REPOSITORY` (ya seteada por el orquestador) → `$WORKDIR`; conectar a la DB de mantenimiento `postgres` con el superusuario, `pg_terminate_backend` de conexiones activas a `odoo`, `DROP DATABASE odoo`, `CREATE DATABASE odoo` (el dump es SQL plano, necesita una base fresca — a diferencia de staging, que ya arranca vacía); `psql -d odoo -f db.sql`; copiar el filestore restaurado `odoo/` → `filestore/.local/share/Odoo/filestore/odoo` (mismo `db_name`, sin rename); `chown -R 100:101` al filestore (Odoo corre como 100:101, no root)
- [x] T010 [US2] Depende de T009 — Escribir `scripts/prod-db-restore.sh` (orquestador de host, bajo `set -e`): guard `[ "$CONFIRM" = "yes" ]` o abortar con mensaje claro y `exit ≠0` sin tocar nada; elegir fuente — `$RESTIC_REPOSITORY_LOCAL` si `LOCAL=yes`, si no `$RESTIC_REPOSITORY_R2` (default), leídas de `env/.env.backup`; `docker build -f Docker/Dockerfile.backup -t odoo-restore-tools:local .`; `docker compose -f Docker/docker-compose.prod.yml stop odoo pgbouncer` (libera conexiones a la DB); `docker run --rm --network odoo-shared --env-file env/.env.backup --env-file env/.env.prod -v odoo-data:/filestore -v /srv/odoo-backups:/backups:ro --entrypoint sh odoo-restore-tools:local /restore-prod.sh`; si el `docker run` sale 0 → `docker compose -f Docker/docker-compose.prod.yml up -d`; si falla, `set -e` corta antes de levantar — Odoo queda parado, no sirviendo datos a medias
- [x] T011 [US2] Depende de T010 — Escribir el target `prod-db-restore` en el `Makefile`: `prod-db-restore: ; ./scripts/prod-db-restore.sh` (recibe `CONFIRM`/`LOCAL` como variables de entorno de la invocación `make prod-db-restore CONFIRM=yes LOCAL=yes`, exportadas automáticamente por `make` al shell de la receta)

## Phase 5: Documentación y reconciliación (US1, US3)

- [x] T012 [US1] Confirmado sin cambios necesarios — los 9 targets ya publicitados coinciden literal con el Makefile real (`make -n <target>` OK para los 9). Actualizar `CLAUDE.md`: confirmar que los targets ya publicitados en "Common Commands" (`prod-up`, `prod-odoo-rebuild`, `prod-odoo-logs`, `staging-up`, `staging-down`, `edge-traefik-restart`, `monitoring-grafana-up`, `backup-backup-run`, `prod-db-restore CONFIRM=yes`) coinciden literal con los del `Makefile` real — ajustar donde diverja
- [x] T013 [P][US1] Actualizar `INSTALL.md`: nota de que el `Makefile` es la interfaz operativa (los comandos `docker compose` directos documentados siguen siendo válidos, el Makefile es equivalente); agregar el runbook del restore de prod como disaster recovery (`make prod-db-restore CONFIRM=yes` → R2, `... LOCAL=yes` → repo local)
- [x] T014 [P][US1] Actualizar `docs/infrastructure-design.md`: reconciliar la sección Makefile (líneas ~195-212) — la referencia a un `./scripts/restore.sh prod/staging` inexistente pasa a los targets reales (`prod-db-restore` → `scripts/prod-db-restore.sh`; `staging-db-restore` → alias de `staging-up`)

## Verification

- [x] VERIFY US1 — `make help` lista los compuestos de stack y los especiales con descripción (confirmado real); `make -n <target-generado>` (dry-run) confirmado para muestra de cada acción (up/stop/restart/logs/pull) en 4+ stacks distintos.
- [x] VERIFY US1 — grep + `make -n` de los 9 targets promitidos en `CLAUDE.md` contra el `Makefile` real — cero divergencias de nombre (T012).
- [x] VERIFY US1/US3 — revisión completa del `Makefile`: ninguna receta tiene guards/ifs/loops; el único target de 2 líneas (`rebuild`) es secuencial sin condicionales, coincide con el patrón ya documentado en CLAUDE.md. Toda la lógica real vive en `scripts/*.sh`.
- [x] VERIFY US2 — **Confirmado con smoke real de punta a punta**: `db` real de prod (no stub, el servicio compose real) + backup real vía `docker/Dockerfile.backup`/`scripts/backup.sh` sin modificar, contra un repo restic LOCAL real y un "R2" de filesystem plano real (mismo patrón que documenta `.env.backup.example`) — ambos poblados con un snapshot real (marker en DB + archivo en filestore). Restore probado contra **ambas fuentes**: `LOCAL=yes` y default (R2) — en ambos casos la DB volvió exactamente a la fila original (el drift post-backup desapareció) y el filestore volvió al contenido original, con ownership `100:101` confirmado. **Guard confirmado real**: sin `CONFIRM=yes`, `./scripts/prod-db-restore.sh` sale `exit 1` sin tocar nada (mensaje claro, uso documentado). **Rollback confirmado real**: apuntando a un repo restic inexistente, `restic restore` sale `exit 10`, `set -e` corta antes del DROP/CREATE — la DB quedó intacta (confirmado por consulta posterior, cero cambios). **Orquestador completo probado real de punta a punta** (`prod-db-restore.sh CONFIRM=yes LOCAL=yes`, incluyendo build de imagen, `stop odoo pgbouncer`, restore, y `up -d` final contra el stack real): el restore de datos se confirmó exitoso (drift eliminado) dentro del flujo completo; el único fallo fue el `docker compose up -d` del servicio `odoo` por un límite de CPU de la VM de Docker Desktop de este sandbox (2 CPUs disponibles, el compose pide `cpus: 4.0`) — limitación del entorno local, no del código (mismo tipo de limitación que `node-exporter`/`rslave` en 006); confirmó además que el script reporta `exit 1` fielmente en vez de tragarse el error. El mount fijo `/srv/odoo-backups` (sin sudo disponible en este sandbox) se sustituyó temporalmente por un path de scratchpad solo durante el test, revertido inmediatamente después — mismo criterio que 004/005/006 con paths de deploy fijos. Todo el entorno de prueba (contenedores, volúmenes, red, imágenes, `env/.env.prod`/`env/.env.backup` de test) limpiado al terminar.
- [x] VERIFY No se crearon archivos fuera de los listados en "File Structure" de `plan.md` — confirmado con `git status` (más el rename `Docker/`→`docker/` del hallazgo T008b, fuera del plan original pero explícitamente autorizado por el usuario).
- [x] VERIFY Sin dependencias nuevas — solo GNU Make + imágenes/scripts ya existentes.
